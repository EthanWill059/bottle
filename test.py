list = [(3,), (4,)]

openids = list
def listToString(s): 
    
    # initialize an empty string
    str1 = " " 
    
    # return string  
    return (str1.join(s))
listToStr = listToString(openids)
print(listToStr)
listToStr = listToStr.replace('(),', "")
listToStr.split()
print(openids)
